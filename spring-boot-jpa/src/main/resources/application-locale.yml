server:
   port: 80
   servlet:
      context-path: /

spring:
   datasource:
      primary:
         jdbc-url: jdbc:postgresql://localhost:5432/postgres
         username: root
         password: mlog123

   jpa:
      generate-ddl: on
